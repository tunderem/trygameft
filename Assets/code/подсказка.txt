//типы данных
    {
        *'тип данных' 'название переменной' = 'содержание переменной'*
        int _lol = 100


        int - целые числа;
        float - дробные числа (добавлять f после числа);
        bool(добавлять is перед названием) - true or false, можно математику и проверку на истинну;
        string - строки;
        var - принимает значение заданного типа данных;
    }


//условия
    { //пример if
        if(условие)
        {
            'код для выполнения';
        }
    { //if условия и знаки
        f - если;
        esle - иначе;
        esle if - иначе если;
            && - и;
            || - или;
            ! - не; //(!true = false)
    }
    { //пример switch
        switch ('имя переменной')
        {
            case ('номер варианта'):
            {
                'код для выполнения'
                break;
            }
            default:
            {
                'код для выполнения';
                break
            }
        }
    } // switch условия и знаки
        switch - если с чем-то совпадёт;
        case - вариант совпадения;
        default - если ничего не подошло;
    }


//